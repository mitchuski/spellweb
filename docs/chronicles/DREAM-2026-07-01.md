# Dream — spellweb — 2026-07-01

## What this is (2-4 lines)
A standing dream-chronicle over `C:\Users\mitch\spellweb` — the spellweb.ai app plus
the knowledge graph (`src/data/nodes.ts` + `edges.ts` + `presets.ts`) that the ATLAS
site projects. It records the graph's current state, today's Hearthold integration, the
gap between the source graph and the live `atlas.localhost` projection (built by
`~/.wiki/skill-fedwiki/build-atlas.js`), and the ordered next actions to "get rolling
tomorrow." Read this first to resume.

## Current state & open threads

**Graph size (source of truth, `src/data`):**
- `nodes.ts`: **773** `id:` definitions total. Of these, **683** are parseable by the
  tooling (single-line, double-quoted literals) and **90** are NOT (the GRIMOIRE v9.2.0
  `term-*` block — still single-quoted/multi-line; see Guide coverage).
- `edges.ts`: **1654** edges (`source:` count = 1654).
- `graph-coherence-audit.mjs` reports: nodes **683** · edges **1654** · 17 node types ·
  53 edge types · **0 errors**.
- `audit-orphans.mjs`: 683 nodes · 681 connected · **2 orphans** · **0 broken edge endpoints**.

**Today's change — the Hearthold KG integration (2026-07-01):**
- Added the **Hearthold cluster: 12 nodes + 26 edges** (nodes 671→683 = +12; edges
  1628→1654 = +26), projecting the cousin-forge's `did:cid` build of the PVM into the graph.
  Block sits under a banner comment at `nodes.ts:1868` and `edges.ts:2446`.
- New nodes (12): `gateway-hearthold` (🏰, `attribution: "cousin-forge"`,
  href `github.com/Flaxscrip/hearthold`), `concept-the-hearth` (the 9th spatial-anatomy
  element · hold-form ⊥ Tower's monument-form), `concept-hearthold-warden` (🛡️ Swordsman,
  built · data plane), `concept-hearthold-witness` (👁️ Mage, built · GenitriX's role),
  `concept-hearthold-sovereign` (🔑 First Person, built · the Signet · control plane),
  `concept-dtg-credentials` (🕸️ VRC/VMC/VIC/VPC/VEC/VWC/RCard + two-faced TRQP registry),
  `concept-evidence-graph` (🧾 never-a-score), `act-tome-x-1` (Tome X Act 1 · `tome: "X"`),
  `conj-c94`/`conj-c95`/`conj-c96` (type `concept`, register `city`, active), and
  `key-hearthold-citykey` (🗝️ Drake Gamers Guild · VRC→κ→City Key byte-matched).
- **`conj-c39` DISCHARGED:** confidence **0.5→0.8**, status provisional→**active**. Its desc
  now reads "DISCHARGED at Tome X — the cousin-forge built the whole PVM triad on did:cid
  (gateway-hearthold)." The discharging edge is `gateway-hearthold → conj-c39 (proves)`.
- **Cousin reuse (no new cast):** the House of Archon is the EXISTING `gateway-archon`
  cousin-forge (`gateway-hearthold → gateway-archon [kin_to]`); `cast-flaxscrip` and
  `cast-genitrix` are existing cousins, wired by `embodies` edges to the Sovereign / Witness
  concepts.
- **Type additions (`src/types/graph.ts`):** `'cousin-forge'` added to the `Attribution`
  union (line 211); `'X'` added to the act `tome?` union (line 263, was capped at IX).

**Open threads (uncommitted / flagged):**
1. **Uncommitted graph edits (today).** `git status` shows `M src/data/nodes.ts`,
   `M src/data/edges.ts`, `M src/types/graph.ts` (vs commit `711b056`). The Hearthold
   cluster + C39 discharge + type additions are all in the working tree, unbuilt-from-source
   but **already present in the live atlas** (see Guide coverage — the atlas was rebuilt today).
2. **NOTE_V10_4_1 Lethae patch — NOW COMMITTED.** Last commit `711b056`
   ("graph: lethae block patch (edges + nodes) + chronicles") lands the graph-side Lethae
   work flagged open in the 06-29 dream. Confirm the **grimoire JSON** side (3 copies +
   re-pin) is separately reconciled; the graph edit alone doesn't re-pin the pinned edition.
3. **90 `term-*` nodes still invisible to the auditor.** The GRIMOIRE v9.2.0 NOTATION block
   (multi-line, single-quoted, ~`nodes.ts` 596–1410) remains unparseable by
   `graph-coherence-audit.mjs` (683 vs 773 id lines). NOTE: the **atlas builder now appears
   to pick them up** (atlas reports 770 nodes vs the auditor's 683) — parser divergence, see
   Notes.
4. **Audit warnings (7, no errors):** 5 duplicate-label collisions
   (`con`/`spell`-emissary-recursion, `con-promisetheory`/`skill-promise-theory`,
   `con`/`skill`-content-addressing, `per`/`cast`-soulbis, `per`/`cast`-soulbae);
   2 shopAnchor mismatches (`cast-the-archivist` `/spells`, `cast-the-librarian` `/guide`).
   Unchanged from 06-29.
5. **2 spell orphans (no edges):** `spell-weather`, `spell-spellweb`. Unchanged. The new
   Hearthold cluster introduced **zero** new orphans.

## Guide coverage (projected vs missing)

Unlike the 06-29 cycle, the atlas is **NOT stale for today's work** — it was rebuilt after
the Hearthold edit. `atlas.localhost` holds **806 pages**; its `welcome-visitors` reports
**770 nodes / 1652 edges**. All 12 Hearthold pages are live.

| guide site | item | status | source path |
|---|---|---|---|
| atlas | `hearthold-the-cousin-forge-build` page + edges | LIVE | `gateway-hearthold` |
| atlas | `the-hearth` | LIVE | `concept-the-hearth` |
| atlas | `the-warden-swordsman-built` / `the-witness-mage-built` / `the-sovereign-first-person-built` | LIVE | `concept-hearthold-warden/witness/sovereign` |
| atlas | `the-decentralized-trust-graph-dtg` | LIVE | `concept-dtg-credentials` |
| atlas | `the-evidence-graph-never-a-score` | LIVE | `concept-evidence-graph` |
| atlas | `the-drake-gamers-guild-city-key` | LIVE | `key-hearthold-citykey` |
| atlas | `tome-x-act-1-the-house-of-archon-answers` | LIVE | `act-tome-x-1` |
| atlas | `c94-...` / `c95-...` / `c96-...` | LIVE | `conj-c94/95/96` |
| atlas | 683 parseable nodes → pages | CURRENT (770 in build view) | `src/data/nodes.ts` |
| atlas | 90 `term-*` NOTATION nodes | picked up by atlas builder, **INVISIBLE to auditor** | `src/data/nodes.ts` ~596–1410 |
| tomes | `act-tome-x-1` + `cast-flaxscrip`/`cast-genitrix` cross-links | needs a Tome X section on the tomes page/site | `build-atlas.js` `crossNarrative()` |
| research | `conj-c94/95/96` + discharged `conj-c39` federate | rebuild sibling for two-way links | `build-atlas.js` welcome federates block |
| skill | `cast-flaxscrip`/`cast-genitrix` cross-refs | OK if sibling pages exist | `build-atlas.js` `crossSite()` |
| grimoire | Tome X / Hearthold narrative (v1.9.1) | separate site — not an atlas projection | cityofmages grimoire (LOCAL) |

## Suggestions (prioritized)

| suggestion | target | why | priority H/M/L |
|---|---|---|---|
| Commit today's Hearthold edits (`nodes.ts` + `edges.ts` + `graph.ts`) with a clear message before the next build overwrites lineage | repo | working tree is dirty; atlas already reflects it but the source commit is the record | H |
| Add a **Tome X section** to the tomes site so `act-tome-x-1` + Hearth get a narrative home (atlas has the node; tomes site likely doesn't) | tomes builder / page | atlas projection exists but the reader-facing tomes narrative lags | M |
| Reconcile the grimoire JSON side of the Lethae v10.4.1 patch + re-pin (graph side is committed at `711b056`; pinned edition may still diverge) | 3 grimoire JSON copies | graph edit alone doesn't re-pin the canon | M |
| Normalize the 90 single-quoted `term-*` nodes to single-line double-quoted literals (OR upgrade `graph-coherence-audit.mjs`) | `src/data/nodes.ts` ~596–1410 | auditor sees 683, atlas sees ~770 — the two parsers disagree; close the gap so the audit covers the whole graph | M |
| Resolve the 2 shopAnchor warnings (`/spells`, `/guide`) | `cast-the-archivist`, `cast-the-librarian` | standing audit warnings; anchors point at non-existent workshop hrefs | M |
| Edge the 2 spell orphans `spell-weather`, `spell-spellweb` | `src/data/edges.ts` | only 2 true orphans; Hearthold added none | L |
| Accept or disambiguate the 5 duplicate labels | `nodes.ts` labels | atlas slugifier disambiguates with `(type)` suffix — cosmetic | L |

## To get rolling tomorrow (ordered next actions)
1. `cd C:\Users\mitch\spellweb && node scripts/graph-coherence-audit.mjs` — confirm still
   683 / 1654 / 0 errors, then `node scripts/audit-orphans.mjs` — confirm 0 broken endpoints.
2. **Commit** the Hearthold edits (`nodes.ts` + `edges.ts` + `graph.ts`) — e.g.
   "graph: Hearthold cluster (12 nodes / 26 edges) + C39 discharge + cousin-forge/tome-X types".
3. Spot-check the atlas pages already exist (they do): `hearthold-the-cousin-forge-build`,
   `the-hearth`, `the-warden/witness/sovereign-...-built`, `the-decentralized-trust-graph-dtg`,
   `the-evidence-graph-never-a-score`, `the-drake-gamers-guild-city-key`,
   `tome-x-act-1-...`, `c94/c95/c96`. If any edit lands later, re-run
   `node C:\Users\mitch\.wiki\skill-fedwiki\build-atlas.js atlas.localhost`.
4. Give **Tome X** a home on the tomes site (new section / Act 1 page) so the narrative
   catches up with the graph.
5. Decide the 90-term parser gap (normalize block vs upgrade auditor) so audit and atlas agree.
6. Fix the 2 shopAnchor warnings so the audit returns fully clean.
7. Commit this dream chronicle.

## Notes & uncertainties
- **Honest-framing (carried):** nothing pushed or published without Mitch's explicit ask —
  the atlas rebuild is a LOCAL host write only, no Cloudflare deploy. `shor_mage` gateway
  material is resource **estimation**, not an attack; vendored SOTA circuits/GPU toolkit are
  other participants' work — credit the method layer only. This dream is read-only reflection;
  I changed nothing in `src/`.
- **Parser divergence (new this cycle):** `graph-coherence-audit.mjs` counts **683** nodes;
  the atlas `welcome-visitors` reports **770**. The ~87 gap is the 90 single-quoted `term-*`
  NOTATION nodes — the atlas builder now seems to parse them while the auditor still does not.
  In the 06-29 dream the atlas saw *fewer* than the auditor (664 < 671); today it sees *more*
  (770 > 683). Worth confirming which parser changed before trusting either count as canonical.
- The atlas `welcome-visitors` edge figure (1652) is 2 short of the source `edges.ts` (1654) —
  minor de-dup / parse difference, not a broken cluster; the Hearthold edges all rendered.
- `attribution: "cousin-forge"` is a genuinely new `Attribution` value in the graph; confirm no
  UI/legend enumerates the union exhaustively (a switch/legend could silently drop the new
  colour/label).
- `conj-c94/95/96` are typed `concept` (not a dedicated conjecture NodeType) but carry
  `conjectureId`/`conjectureStatus`/`conjectureConfidence`/`conjectureRegister` — consistent
  with the existing C-band convention in the file.
- The Hearthold cluster is internally well-connected (every one of the 12 nodes has ≥1 edge);
  the only two graph orphans remain the pre-existing `spell-weather` / `spell-spellweb`.
- I did NOT run `tsc` / `vite build` myself (prompt reports them clean); the coherence +
  orphan audits I did run are both green.
