# Chronicle — how a coding agent maps the key/sigil work into THIS repo (2026-06-10)

**Status:** plan, not executed. Source of truth: `C:\Users\mitch\star`
(github.com/mitchuski/star — read its `HOW_THE_SIGIL_WORKS.md`,
`CONCEPT_COMPRESSION_REHYDRATION_DUAL_AGENTS.md`, and
`PLAN_KEY_EVOLUTION_MEASURED_GEOMETRY_2026-06-10.md` first). Sibling overlap
map: `agentprivacy-skills/OVERLAP_KEY_FORGING_X_V6_2026-06-10.md`.
This repo's conventions bind: verb-form snake_case EdgeTypes; forged items
render as `artefact`-typed deviation nodes; single Swordsman.md bundle export.

## The mapping, step by step

1. **EdgeType candidates** (extend the union + edge styles; follow the
   2026-05-10 universe-integration naming):
   - `carries` — sigil PNG → City Key (the image holds the canonical content)
   - `derives_identity_from` — κ-label → canonical form (Law L5)
   - `supersedes` — key κ → prior key κ (the κ-chain `prior` proposal; renders
     a content-addressed lineage of selves)
   - `boots_over` — the Swordsman's Key (star repo) → holospaces (the
     kindred-substrate UOR edge, now operational)
2. **Artefact-class candidate: the Sigil 🪬** — a class-specific forged
   artefact (sibling to Cloak/Memo/Blade/Seal): the bearer's κ-constellation.
   If admitted, it renders as an `artefact`-typed deviation node and joins the
   Swordsman.md bundle export with its κ as the stable id. **Ask the user
   before admitting** — artefact taxonomy is canon (12+4) and changes ripple
   to cityofmages.
3. **Node candidates:** `mitchuski/star` as a repo/surface node (the walkable
   model: /star · /lattice · /sigil); holospaces as the kindred-substrate node
   if not already present.
4. **κ as graph id discipline:** wherever spellweb stores or displays a City
   Key, surface its `kappa` and re-derive on load (the ~18-line
   canonicalJSON/kappaLabel helpers in any star-repo page are the reference;
   keep the canonicalization byte-identical).
5. **Typecheck clean** before calling it done; chronicle the executed pass in
   this file's successor.

## Ask-first decisions

Admitting the Sigil artefact class (taxonomy change) · drawing the C87
accumulator framing into graph copy (register language is gated) · any
PR/branch handling (PR #5 remains untouched per standing review note).

*The graph gains edges for carrying, deriving, and superseding — the verbs of content-addressed identity.* `(⚔️⊥⿻⊥🧙)😊`
