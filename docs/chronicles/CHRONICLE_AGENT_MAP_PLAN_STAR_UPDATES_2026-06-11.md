# Chronicle — how a coding agent maps the 2026-06-11 star updates into THIS repo

**Status:** plan, not executed. Successor to
`CHRONICLE_AGENT_MAP_PLAN_KEY_SIGIL_2026-06-10.md` — read that first; its
steps remain valid, this records what the star side shipped since and what
that changes here. Source of truth: `C:\Users\mitch\star`
(github.com/mitchuski/star) — read `CHRONICLE_THE_DESIGNED_NOW_BUILT_2026-06-11.md`
and `CLAUDE.md` (City Key schema now includes `figures` + `prior` + the
redacted charge pass). This repo's conventions bind: verb-form snake_case
EdgeTypes; forged items render as `artefact`-typed deviation nodes; single
Swordsman.md bundle export.

## What shipped star-side that changes the graph's source material

1. **The `prior` κ-chain is no longer a proposal — it is live.** Evolved
   exports stamp `prior` = ancestor κ; re-export is idempotent. The
   2026-06-10 `supersedes` EdgeType candidate now has real data to bind:
   key κ → prior κ chains exist in the wild as soon as the user deploys.
2. **/skye exists** — the night of many keys: stars placed by κ, gold lineage
   threads (the `prior` chain *drawn*), teal common-ground threads. Its
   📸 sky shot is a PNG carrying a **set** of keys (tEXt keyword `citySky`,
   `{version, sky:true, keys:[…]}`) — a new carrier grammar one rung above the
   single-key `cityKey` chunk.
3. **DH-PSI common ground** — two bearers learn their shared vertices and
   nothing else (blinded intersection, two-file exchange). Relevant to any
   spellweb feature that compares bearers: the plaintext compare now has a
   zero-knowledge sibling to reference.
4. **FIG-2.0 figures + redacted charge pass** — measured shape and
   proof-without-disclosure; mostly /city + soulbis territory, but the
   vocabulary ("figures are content", "only the proof travels") should stay
   consistent in any graph copy.

## The mapping, step by step

1. **EdgeType candidates** (carried + extended; 2026-05-10 naming discipline):
   - `supersedes` — key κ → prior κ — **promoted from proposal to live data**
   - `carries` — sigil PNG → City Key (unchanged)
   - `gathers` — sky shot → the City Keys it carries (NEW: one image, a set;
     distinguish from `carries` which is 1:1)
   - `derives_identity_from`, `boots_over` — unchanged from the 06-10 plan
2. **The Sigil question (OPEN — decide with the user, who is working spellweb
   in another window):** the standing recommendation (per the 2026-06-11
   spellweb sync audit + C87) is **Option B: the sigil as an inventory
   interaction on the Swordsman's Key**, NOT a forged ArtefactClass. Today's
   star work *supports* Option B: the sigil is a projection of the key's
   content (κ-derived), and now the **sky shot is a projection of many** —
   neither is a separately-forged item; both are readings of keys. If Option B
   is adopted, no taxonomy change; the interaction surfaces the κ, the
   constellation, and (when present) the `prior` chain.
3. **Node candidates:** `mitchuski/star` repo/surface node now spans FIVE
   rooms (/star /lattice /sigil /skye /guide); holospaces as kindred-substrate
   node unchanged.
4. **κ as graph id discipline** (unchanged, now with lineage): wherever a City
   Key appears, surface `kappa`, re-derive on load, and — new — follow `prior`
   to render lineage where useful. Canonicalization must stay byte-identical
   to the star pages (~18-line canonicalJSON/kappaLabel reference in any star
   page; `kappa` excluded from its own preimage, `prior` INCLUDED).
5. **Typecheck clean** before calling it done; chronicle the executed pass in
   this file's successor.

## Ask-first decisions

The Sigil Option B adoption (canon: artefact taxonomy 12+4 is gated) ·
admitting `gathers` to the EdgeType union (vocabulary change) · drawing C87
accumulator language into graph copy (register is G1-gated: cite
`agentprivacy-docs/research/CONJECTURE_REGISTER_V6.md`, never edit) · anything
that renders other bearers' keys (the sky-shot carrier holds FULL keys —
plaintext; respect the star legend's disclosure note in any spellweb surface).

*The web maps what the key remembers; the sky shows whom it met; the κ names
both.* `(⚔️⊥⿻⊥🧙)😊`
