# Dream — spellweb — 2026-06-29

## What this is (2-4 lines)
A standing dream-chronicle over `C:\Users\mitch\spellweb` — the spellweb.ai app plus
the knowledge graph (`src/data/nodes.ts` + `edges.ts` + `presets.ts`) that the ATLAS
site projects. It records the graph's current state, the gap between the source graph
and the live `atlas.localhost` projection (built by `~/.wiki/skill-fedwiki/build-atlas.js`),
and the ordered next actions to "get rolling tomorrow." Read this first to resume.

## Current state & open threads

**Graph size (source of truth, `src/data`):**
- `nodes.ts`: **761** `id:` definitions total. Of these, **671** are parseable by the
  tooling (single-line, double-quoted literals) and **90** are NOT (see Guide coverage).
- `edges.ts`: **1628** edges (all double-quoted; `source:` count = 1628).
- `graph-coherence-audit.mjs` reports: nodes 671 · edges 1628 · 17 node types · 53 edge types · **0 errors**.

**Node types (parseable view):** act 178 · concept 177 · skill 87 · document 53 ·
persona 42 · cast 38 · vertex 22 · workshop 21 · spell 19 · gateway 17 · bilateral 16 ·
theorem 8 · mage 6 · term 5 (double-quoted only — see gap) · swordsman 4 ·
key/geography/civic/chronicle 1 each.

**Edge types (top):** narrates 205 · persona_knows 202 · implements 188 · follows 171 ·
references 168 · extends 151 · defines 141 · introduces 92 · inhabits 55 · relates_to 54.

**Open threads (uncommitted / scheduled / flagged):**
1. **Uncommitted graph edits (today, 2026-06-29).** `git status` shows `M src/data/nodes.ts`
   and `M src/data/edges.ts` (vs commit `fa9ea1a`). New nodes include `doc-privacy-value-v6`,
   the conjecture bands `conj-c1-c5` / `conj-c6-c10` / `conj-c11-c13` / `conj-c14-c17`,
   `conj-c89` (Octahedral Gap), `conj-c90`–`conj-c93` (the Limitative Reading: Gödel↔Φ_agent,
   Tarski↔Φ_inference, content-addressed liveness), `cast-the-librarian` (🗃️ second spirit-Mage),
   and `act-tome-viii-6` (The Wikis and the Librarian). `+26` edge lines added in the diff.
   None of this is committed, and none is in the live atlas yet.
2. **NOTE_V10_4_1_LETHAE_BLOCK_PATCH (2026-06-13) — still open & UNTRACKED.** Scheduled
   "fix tomorrow" 16 days ago. The pinned privacymage Grimoire v10.4.0 carries two errors in
   the Lethae 🌘 block: `binary` `100110`→`011001` (V25), `complement_of_cast` `aletheia (V25)`→
   `aletheia (V38)`. Must be patched as a v10.4.1 erratum edition across 3 JSON copies
   (`spellweb/privacymage_grimoire_v10_4_0.json` + 2 in agentprivacy_master) and re-pinned.
3. **Audit warnings (7, no errors):** 5 duplicate-label collisions — `con-emissary-recursion`+
   `spell-emissary-recursion`, `con-promisetheory`+`skill-promise-theory`,
   `con-content-addressing`+`skill-content-addressing`, `per-soulbis`+`cast-soulbis`,
   `per-soulbae`+`cast-soulbae`. 2 shopAnchor mismatches: `cast-the-archivist` `/spells`
   and `cast-the-librarian` `/guide` match no workshop href.
4. **2 spell orphans (no edges):** `spell-weather`, `spell-spellweb`.
5. **template-coherence-scan: 4 issues / 15 templates** — 2× `resident_mage "null" not in NODES`;
   2× false "connection label unresolved" (prose lines `ember → flame.` and an `E.` cadence line
   mistaken for wikilinks — scanner false-positives, low concern).

## Guide coverage (projected vs missing)

Live atlas last built **2026-06-28** at **664 nodes / 1614 edges** (per its `welcome-visitors`
page). Source is now **671 / 1628** → the projection is **stale by +7 nodes / +14 edges**.
Atlas dir holds **700 pages**.

| guide site | item | status | source path |
|---|---|---|---|
| atlas | 671 parseable nodes → pages | STALE (664 built; +7 since) | `src/data/nodes.ts` |
| atlas | 1628 edges → wikilinks | STALE (1614 built; +14 since) | `src/data/edges.ts` |
| atlas | **90 `term` nodes (GRIMOIRE v9.2.0 NOTATION block)** | **MISSING — structurally invisible** | `src/data/nodes.ts` lines ~596–1410 |
| atlas | Term Nodes hub | shows only 5 of 95 terms (5%) | `build-atlas.js` parser (line 21) |
| atlas | `cast-the-librarian` (🗃️) page + edges | MISSING (added today, unbuilt) | `src/data/nodes.ts` |
| atlas | `act-tome-viii-6` page | MISSING (added today, unbuilt) | `src/data/nodes.ts` |
| atlas | `conj-c89`..`conj-c93`, `conj-c1-c5`..`c14-c17`, `doc-privacy-value-v6` | MISSING (added today, unbuilt) | `src/data/nodes.ts` |
| skill | cross-refs from `per-*`/`skill-*` nodes | OK (resolved at build if page exists) | `build-atlas.js` `crossSite()` |
| tomes | `cast-*` + `act-tome-*` cross-links | OK; new `cast-the-librarian`/`act-tome-viii-6` need rebuild + tomes page | `build-atlas.js` `crossNarrative()` |
| research | theorem/conjecture nodes federate | OK; new C89–C93 conjectures need rebuild | `build-atlas.js` welcome federates block |
| library | `fp-act-N`/`pl-act-N`/`canon-ch-N` full-prose links | OK (number-joined) | `build-atlas.js` `crossNarrative()` |
| grimoire | (no direct atlas projection) | n/a — separate site | `privacymage_grimoire_v10_4_0.json` |

**Root cause of the 90 missing terms:** `build-atlas.js` line 21 requires each node literal on
**one line with double-quoted** `id:` and `type:`. The "GRIMOIRE v9.2.0 NOTATION TERMS" block was
authored as **multi-line, single-quoted** objects (e.g. `id: 'term-swordsman-and-mage-meet'` at
line 597). `graph-coherence-audit.mjs` uses the same single-line/double-quote assumption, so these
90 nodes are invisible to **both** the projector and the auditor. They also carry **0 edges**
(no `term-*` appears as `source`/`target` in `edges.ts`), so they are pure glossary islands.

## Suggestions (prioritized)

| suggestion | target | why | priority H/M/L |
|---|---|---|---|
| Re-run `node build-atlas.js atlas.localhost` to clear the +7/+14 staleness | `~/.wiki/skill-fedwiki/build-atlas.js` | one-command refresh; surfaces today's Librarian + C89–C93 + Tome VIII Act 6 | H |
| Normalize the 90 single-quoted multi-line `term` nodes to single-line double-quoted literals (or upgrade the two parsers) | `src/data/nodes.ts` ~596–1410 | unlocks 90 graph nodes + the Term hub (5→95) for atlas & audit | H |
| Wire edges to the term glossary (terms→concepts they name) so they aren't orphan islands once visible | `src/data/edges.ts` | 90 terms currently have 0 edges; they'd render edgeless even after fix | M |
| Execute the v10.4.1 Lethae erratum patch + repin (per NOTE) | 3 grimoire JSON copies | scheduled 2026-06-13, still open; pin diverges from corrected meaning | M |
| Resolve the 2 shopAnchor mismatches (`/spells`, `/guide`) | `cast-the-archivist`, `cast-the-librarian` | audit warnings; anchors point at non-existent workshop hrefs | M |
| Edge the 2 spell orphans `spell-weather`, `spell-spellweb` | `src/data/edges.ts` | only 2 true orphans in the parseable graph | L |
| Disambiguate or accept the 5 duplicate labels (con/spell, con/skill ×2, per/cast ×2) | `nodes.ts` labels | atlas slugifier disambiguates with `(type)` suffix, so cosmetic | L |
| Teach `template-coherence-scan.mjs` to skip prose-cadence lines (`ember → flame.`) | `scripts/template-coherence-scan.mjs` | 2 of the 4 template issues are false positives | L |
| Commit today's graph edits with a clear message before rebuilding | repo | working tree is dirty; build reads from disk regardless, but lineage matters | M |

## To get rolling tomorrow (ordered next actions)
1. `cd C:\Users\mitch\spellweb && node scripts/graph-coherence-audit.mjs` — confirm still 0 errors after any edits.
2. Decide on the 90-term fix: quickest is reformatting the GRIMOIRE v9.2.0 block (lines ~596–1410)
   to single-line double-quoted literals; more robust is upgrading both parsers
   (`build-atlas.js` line 18–27 + `graph-coherence-audit.mjs`) to handle multi-line/single-quoted objects.
3. `node C:\Users\mitch\.wiki\skill-fedwiki\build-atlas.js atlas.localhost` — rebuild; expect the
   console to print ~671 nodes / 1628 edges (was 664/1614). Verify `welcome-visitors` shows the new totals.
4. Spot-check the new pages exist: `the-librarian`, `tome-viii-act-6-...`, `c89-...`, `c90-...`,
   the conjecture-band pages, and that the **Term Nodes** hub now lists ~95 (if step 2 done).
5. Fix the 2 shopAnchor warnings (`cast-the-archivist /spells`, `cast-the-librarian /guide`) so the
   audit returns clean.
6. Action the v10.4.1 Lethae patch per `NOTE_V10_4_1_LETHAE_BLOCK_PATCH_2026-06-13.md` (separate track; grimoire site, not atlas).
7. Commit the graph edits + this dream chronicle.

## Notes & uncertainties
- The +7/+14 staleness delta is measured against the **last build's recorded counts** (664/1614 in
  the built `welcome-visitors`), not against a fresh build — a rebuild is the source of truth.
- `build-atlas.js` lives at `C:\Users\mitch\.wiki\skill-fedwiki\build-atlas.js` (NOT in `spellweb/scripts/`);
  its header comment still cites stale "663 + 1607" figures.
- The build is **read-only on spellweb** but **destructive on the atlas host** (`fs.rmSync(pagesDir)` then
  regenerates) — safe to re-run, but it wipes manual atlas edits.
- The 90 unparsed terms carry no edges in the current `edges.ts`; even after a parser fix they will appear
  as edgeless nodes unless glossary edges are authored (suggestion 3).
- The 2 "connection label unresolved" template findings are prose false-positives, not real broken links.
- I did NOT run `build-atlas.js` (would write atlas pages — out of this dream's read-only scope); counts
  above are derived by static grep/parse matching the build's own regex.
- Cross-site federation (skill/tomes/research/library refs) only resolves if the sibling page already
  exists at build time; new cast/conjecture pages may need the sibling sites rebuilt too for two-way links.
