# Dream — spellweb — 2026-08-27

*Read this first to resume. The BGIN IKP lane — the co-chair's other guild — got its first
dedicated home: `~/BGIN_IKP`, founded by a dream cycle that swept the machine and gathered
eight months of scattered tellings into one hall the day before the working-group call.
Continues the DTG arc's sibling thread (DREAM-2026-08-12); nothing pushed, nothing minted in
the KG data itself — this record names the candidate nodes.*

## What this is

The KG has long carried the DTG ZKP lane as first-class structure and BGIN only as an atlas
concept node. Today the asymmetry was measured and found to be real *in the filesystem too*:
the DTG workbenches contain **zero** literal mentions of BGIN / IKP / GDC / Block 15. The two
co-chair lanes touch **only through documents** — the 28 Aug call agenda, the memory files,
and the *Proof of Human* convergence note. In KG terms: no shared directories, only shared
edges. The disclosure discipline is working as architecture.

The dream cycle's cargo, node-shaped:

- **`~/BGIN_IKP`** — new hall: seven rooms (block15 · gdc26 · pqc · personhood ·
  zkp-dtg-bridge · wg-ops · chronicles), 23 canonical copies + 1 web archive, provenance
  manifest (`SOURCES.md`) recording every origin and dedupe ruling.
- **ADR-001 *Community-Anchored Proof*** (Proposed 2026-08-25) — evaluated at the seam and
  archived locally for the first time. Its clause 3 (the relationship's issuer is also a
  community member) *is* the verifiable-edge question; its registry clauses land on the
  deferred registry–ZK charter; its full-unlinkability clause needs the trade-curve
  clarification (scoped nullifiers = governed, declared linkability).
- **Version rulings as edges, not copies:** liveness requirements v0.4 = current (found in
  Downloads, not the workbench — the workbench clone may need a pull); the as-circulated WF
  convergence note = canonical of six variants; the `(2)` PQC docx = canonical of four.
- **The agenda** (28 Aug, 12:00 UTC, PQC locked as focus; GDC26 corrected to 1–3 Sep from the
  convergence note's own sequencing) — the document that forced the consolidation.

## Open threads

1. **Candidate mints:** `concept-bgin-ikp-kb` (the hall) with edges `anchors_to` →
   `concept-bgin` (atlas), `cites` → the convergence note, `bridges` → the DTG ZKP lane
   nodes; and a `community-anchored-proof` concept beside the existing DTG credential nodes.
   Minting = a deliberate KG pass, not this dream.
2. **The bridge is one-directional on paper.** The convergence note cites DTG publicly; no
   DTG artifact cites BGIN. If the ADR comments land in TF threads, the first reverse edge
   appears — watch for it.
3. **WP-09 *Separation of Duties*** sits venue-stamped for Block 15 in the Programme
   pipeline; its KG twin already exists in the papers weave. The submission (≈8 Oct) is the
   First Person's act.

⚔️🏛️⊥🧙📜|😊
