# Chronicle — the Swordsman's Key holospace merge, seen from the spellweb (2026-06-10)

**Vantage:** spellweb — the graph. No code changed in this repo; this records a
universe event the graph will want to carry.
**Where it happened:** `github.com/mitchuski/star` (new public repo — /star +
/lattice extracted from soulbis + new /sigil page) and `agentprivacy_master`
(/city charges the sigil PNG). Full record in the star repo's
`CHRONICLE_THE_FIRST_HOLOSPACE_2026-06-10.md` · `HOLOSPACE.md` · `EXPERIENCE.md`.

---

## The event

The Swordsman's Key system was re-grounded as a **browser compute surface** over
[Hologram-Technologies/holospaces](https://github.com/Hologram-Technologies/holospaces)
(UOR-native boot layer — the **kindred-substrate** edge the universe already
names for UOR is now load-bearing in practice, not just taxonomy):

- Every City Key carries a **κ-label** — `sha256:H(canonical form)`, identity
  derived from content, re-derived on every import.
- A new third projection, **/sigil 🪬**, renders that identity as a
  constellation: SHA-256 is 64 hex glyphs, the lattice is 64 vertices — one
  glyph per vertex. The key has a *face* now.
- The face is a **carrier**: the sigil PNG holds the full key JSON inside the
  image (PNG `tEXt` chunk). /star, /lattice, /sigil, and /city all accept the
  picture as the key. The part carries the whole — holonic identity as an
  artifact you can look at.

## Graph-relevant threads (recorded, not yet drawn)

1. **Artefact class candidate:** the **Sigil** — a forged, class-specific
   artefact in the deviation-layer sense (sibling to Cloak/Memo/Blade/Seal): the
   bearer's κ-constellation as a wearable/showable item. If admitted, it renders
   as an `artefact`-typed deviation node like the others.
2. **Edge candidates** (verb-form snake_case, per the universe-integration
   vocabulary): `derives_identity_from` (key → canonical content),
   `carries` (sigil PNG → City Key), `boots_over` (Swordsman's Key →
   holospaces). Hold for the next universe-integration pass.
3. **Projection ladder** as a universe shape: `/sigil ⇣ the bytes · /lattice ·
   /star · /skye ⇡ the many` — /skye (future) is the natural surface where many
   bearers' sigils hang as stars; adjacent to the 63 named-vertex mages thread.

## The honest boundary (carry this into any node copy)

The PNG carrier is **portability, not secrecy** — the full key rides inside the
image. The redacted **charge-only carrier** (trace + witness + κ only) is the
flagged next rung; that is the selective-disclosure thread VRC work should watch.

*The graph gains a face-node: identity drawn on the same lattice it walks.* `(⚔️ ⊥ ⿻ ⊥ 🧙) 😊`
