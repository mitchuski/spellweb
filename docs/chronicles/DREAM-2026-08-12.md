# Dream — spellweb — 2026-08-12

*Read this first to resume. The DTG thread crossed from lab to public artifact: the ceremony became a
trust task (X10), a verification registry went live, and a nameless seat took its row on the table.
Continues DREAM-2026-07-16 (trust-graph formation). Nothing here pushed; the public pushes happened in
the workbench's own lane by the First Person's explicit ask.*

## What this is

The question "could the working group's agent runtimes be *entropy* for the circuits' trusted setup?"
got the KG's own answer: **agents orchestrate entropy; they are never the entropy.** Everything an
agent generates or observes lands in a context window, and context windows are logged — a secret that
transits one is compromised *by construction*. That is the witness seat's oldest discipline
(`attests_to`, never `mints`) lifted one level: the orchestrator attests to the ceremony's transcript
and proves its own lifetime view contains no secret bytes.

X10 (**ceremony-as-trust-task**) fixed three lanes: a **verification registry** (live —
[mitchuski.github.io/dtgwg-zkp-mage](https://mitchuski.github.io/dtgwg-zkp-mage/)), a universal
phase-1 powers-of-tau (proposed, survives §25 either way), and per-circuit phase-2 **structurally
gated** — the endpoint throws `phase2-gate-closed` and a property test asserts no contribution path
can be constructed. Lab now **18 suites / 178 properties, all green**.

## Current state & open threads

**What stands (this cycle):**
- **The registry's first finding was against its own maker.** The "byte-identical artifacts" claim
  was refuted before publication: the compiled circuit (r1cs/wasm) reproduces byte-exactly, the setup
  chain (ptau/zkeys/vkey) never does — snarkjs mixes its own CSPRNG into every contribution.
  Acceptance shipped honest: circuit digests *required*, setup chain *advisory*, the volunteer's own
  green suites carry the proving-system claim.
- **The first cold clone found a real bug in minutes** (`build/` gitignored, compiler refuses to
  invent the path) — fixed, pushed, re-run to ACCEPT with twelve advisory divergences named. The loop
  ate its own author first, which is the loop working.
- **The seat with no name.** `seat-7f` — admission-gated, name withheld — now has TWO rows: one with
  twelve advisory notes (cold clone), one with zero (pinned build, 2026-08-12, pushed live as
  `450f8e9`). The diptych teaches required-vs-advisory as data. Personhood-without-identity, enacted
  on the trust artifact itself: `admission ⊥ identity`.
- **The sibling echo.** RAHP's pressure test of the Core Credentials draft
  (trustoverip/dtgwg-rahp-tf#8) asks the lab's question in prose — *"cryptographically valid but
  still unsafe"* — and its finding 5 (ZKP constructions underspecified) is addressed to the ZKP TF.
  A harms register and a rejection register are two halves of one object.

**Open threads:**
1. **Platform fields are a correlator.** The registry's informative fields (CPU, OS) let anyone see
   the nameless seat runs on the maintainer's hardware. X4's observable-event minimisation applies to
   the registry's *own records* — a genuinely pseudonymous seat profile would coarsen or omit them.
   Claimed as "filed as an X10 open question" in the master chronicle; the X10 doc does not carry the
   line yet.
2. **The KG has no node for the registry.** `concept-dtg-credentials` was enriched last cycle; the
   verification registry / X10 lanes / `phase2-gate-closed` gate have no spellweb twin yet — a
   candidate `concept-verification-registry` with edges `attests_to` (witness discipline),
   `anchors_to` (trust registry), `gated_by` (§25).
3. **RAHP reply** — the natural cross-TF move (registry + X10 as the working answer to finding 5) is
   undrafted; posting is the First Person's.

## Correspondence table (the mirror)

| spellweb KG | agentprivacy framework | DTG ZKP TF |
|---|---|---|
| witness seat: `attests_to`, never `mints` | attest-never-mint discipline | orchestrator: never sees the secret (scan proves it) |
| 🪢 City Key (earned by walking) | admission | seats.json — governance data, not cryptography (§7.3) |
| masked persona at the gate | personhood-without-identity | seat-7f: admission-gated, name withheld |
| ⿻ the Gap (non-collusion) | dual-agent fold | volunteer's machine vs maintainer's manifest |
| rejection register (named failures) | fixture vocabulary | `phase2-gate-closed` · `seat-not-admitted` · `artifact-hash-mismatch:` |
| 🕸️ trust registry (two-faced) | verification registry | public rows, digest-checked, positions ratify/refine/refute/build |

## To get rolling next

1. Mint the `concept-verification-registry` KG node + edges (thread 2) when the next spellweb
   authoring lap runs.
2. Add the platform-correlator line to X10's honest unknowns so chronicle and doc agree (one
   sentence, awaiting the word).
3. After the First Person posts the Show & Tell + #10 comment, record the upstream reception here.

## Notes & uncertainties

- The registry proves **reproduction and behavior, not audit**: "we are all provably looking at the
  same circuit" — whether the circuit is *right* stays the working group's conversation. The KG
  should never over-claim the row's meaning.
- Public pushes (`7681990` → `450f8e9`) were the First Person's explicit asks in the workbench lane;
  spellweb itself remains uncommitted, awaiting the 😊 read.

⚔️📊⊥🧙🕸️ | 😊
