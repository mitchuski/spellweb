# DREAM · 2026-09-05 · The Flock Names a Face

*KG voice. Compresses to the master chronicle `agentprivacy_master/docs/chronicles/2026-09-05_the-book-takes-a-name.md` (unsigned). Nothing deployed.*

## Nodes minted (staged)

- **`zk-tale-32`** · act · *ZK Tale 32: The Flock 🌗* · spellbook `zero_knowledge` · Blade 61 (`111101`) / seated V47 · vertex ⟨1,0,1,1,1,1⟩ — Delegation alone dark · V(π,t) [C, A_h(τ), Value] · proverb: *the cheapest proof is of the hash you already have*.

## Nodes revised (staged)

- **`spellbook-zk`** · counts → 32 tales · 16 named blades · 48 frontier vertices.

## Edges wired (staged)

- `zk-tale-31 → zk-tale-32` **follows** — the Quest's second step.
- `spellbook-zk → zk-tale-32` **defines**.
- `zk-tale-32 → zk-tale-12` **references** — the Folding Path (23) is the first term of the counting triple.
- `zk-tale-32 → zk-tale-31` **references** — Lethe (38) is the second; 23 + 38 = 61.
- `zk-tale-32 → zk-tale-24` **references** — the mixer's commit / prove-membership / nullify shape, now over a standard hash.
- `zk-tale-32 → zk-tale-18` **references** — the Toxic Waste Dragon: this blade has no setup to guard.
- `zk-tale-32 → con-a-h-tau` **extends** — the horizon that survives the quantum break.
- `zk-tale-32 → doc-zk-blade-forge-spec` **references** — moon phase and blade encoding.

## What the graph learned

A frontier tale is identified by its vertex first and its number second; the number is a rendering under a convention,
and the corpus carries two (tale d6-MSB, lattice-lock d1-MSB) — stated together until ruled. The counting confirmed the
walking: Folding + Lethe = Flock, ratio ≈ φ; the complement (pure Delegation) is unnamed, so a pair opened. Outside the
graph the same tale's Technical Bridge entered the DTG ZK Book as primer section P5 and as the `flock` stack —
knowledge without the telling.

⚔️📊⊥🧙🕸️|😊

---

# DREAM · 2026-09-05 · second dream · The Guide Becomes a Lattice

*KG voice. Compresses to the master chronicle `agentprivacy_master/docs/chronicles/2026-09-05_the-agent-walks-the-guide.md` (unsigned). Nothing committed, nothing deployed.*

## Nodes minted (staged)

- **`concept-page-posture`** · concept · *Page Posture* · domain shared · layer knowledge · desc: a page's six-bit stance on the sovereignty lattice, carried ON the page as a `posture` story item (the tileglyph/starpath shape) so a fork carries it; the star chart seats by it, strand isolation reads it. Bit canon d1 protection=32 … d6 value=1, held on five surfaces. · proverb: *the stance is on the page*.
- **`concept-the-walk-as-content`** · concept · *The Walk as Content* · desc: `key_evolve` appends an ordered walk (slug · vertex · PSI element `sha256(vertex|slug|sorted links)`) to the City Key's additive `walks` field, chains `prior`, restamps κ — pure, deterministic, so a constellation here and a walk on the guide yield one κ. · proverb: *the same walk, the same name*.
- **`concept-vta-record`** · concept · *The VTA Record* · desc: what a Verifiable Trust Agent publishes — public key · did:key · κ · prior · signing time · walk count · VRC commitments — signed ed25519 over its canonical form, set down BESIDE the key so the κ rule never changes. · proverb: *the signature sits beside the key*.
- **`gateway-the-swordsman-process`** · gateway · *The Swordsman ⚔️ (process)* · domain swordsman · desc: a second MCP process, no LLM, seed + fixed policy + append-only ledger; signs only chained evolutions of its own bearer's key, refuses rewrites, re-signs, foreign bearers, lying κ, unbaked steps, and rate-limits from the ledger. Φ_agent as a process boundary.
- **`gateway-agentprivacy-mcp`** · gateway · *agentprivacy-mcp (the Mage's tools)* · domain mage · desc: the guide, the lattice and the City Key as MCP read tools over the BAKE (never the live farm): search as an ordered walk · page · neighbours as named moves · lattice move · derive · evolve · render · verify.
- **`concept-evolved-since`** · concept · *evolved_since(t)* · desc: the first liveness predicate — a valid VTA record signed after t; a stale κ is a stale agent; establishes memory (continuity) only; the predicate atlas's first panel.

## Nodes revised (staged)

- **`key-city-key`** (the City Key carrier) · fields → `+ walks[]` (additive, v1 extensibility clause) · did:key recipe now derived in code, not only pinned in a comment.
- **`gateway-guide-agentprivacy-ai`** · the star chart seats postured stars at their vertex; per-page record `pages.json` (2,207 rows, 34 postured, 18 vertices).

## Edges wired (staged)

- `<node> → <guide page>` **guide** — 682 of 726 nodes now carry the slug of their guide page (`src/data/guide-bridge.ts`): concept 177/204 · act 177/180 · skill 86/87 · persona 46/46 · cast 38/38 · workshop 25/25 · vertex 21/26 · chronicle 0/3. Rule: identity is the slug, never the host.
- `concept-the-walk-as-content → key-city-key` **keys_to** — the walk lights the vertices it seats.
- `concept-vta-record → key-city-key` **references** — signed κ, not signed key.
- `gateway-the-swordsman-process → concept-vta-record` **defines** — the only minter.
- `gateway-agentprivacy-mcp → gateway-the-swordsman-process` **synced_with** — hands keys across the process boundary, gets records back, never a seed.
- `concept-evolved-since → concept-vta-record` **extends** — the predicate is a reading of the record's `at`.
- `gateway-the-swordsman-process → concept-credential-v1` **references** — the same ed25519 AgentCard identity the City's board admits with; the Portal's canonical-JSON rule is κ's rule.
- `concept-page-posture → vertex-v36` **inhabits** — the private-knowledge-network page's first seat (protection + connection).

## What the graph learned

The graph's older six lines (`NodeDimensions`: key custody … trust boundary) and the sovereignty lattice's six (protection … value) are two coordinate systems on the same sixty-four; the bridge maps nodes to pages, it does not reconcile the axes — 102 vertex-bearing nodes point at pages that carry no posture yet, so the drift audit is silent until tomes, city and atlas are postured. The walk became content and the signature moved beside the key; both were proven by equality of κ rather than by inspection. The board's empty `cityKey` slot was the seam two windows met at without speaking.

⚔️🗝️⊥🧙🕸️|😊


---

# DREAM · 2026-09-05 · The Lab Opens Its Door (second dream of the day)

*KG voice. Compresses to the master chronicle `agentprivacy_master/docs/chronicles/2026-09-05_the-lab-opens-its-door.md` (unsigned). Nothing deployed; the domain is parked.*

## Nodes minted (staged)

- **`surface-agentprivacy-org`** · surface · *agentprivacy labs, agentprivacy.org* · five doors (deploy · services · research · concepts · fund) · style = soulbis × agentprivacy mix · gate 31/31 GO · not deployed.
- **`org-agentprivacy-labs`** · organisation · *an AI cybersecurity research lab* · does four things: installs the open stack · runs the harness on hard problems · takes fixed-scope engagements · publishes the programme.
- **`svc-autoresearch-engagement`** · service · proposer sweeps → refuter → judge; findings covered · narrowed · survives; the kill ledger is the artefact.
- **`cand-agentprivacy-foundation`** · candidate concept (UNNUMBERED) · *an open-source agentic cybersecurity ecosystem; the lab is the first step* · entered today from the First Person; no register number.

## Edges wired (staged)

- `org-agentprivacy-labs → surface-agentprivacy-org` **publishes**.
- `surface-agentprivacy-org → doc-provider-brief-2026-09-01` **derives-from** — every load-bearing claim traces to the brief or a public README.
- `org-agentprivacy-labs → kyra-gate` · `→ dual-agent-harness` · `→ dtgwg-zkp-mage` · `→ agentprivacy-guide` · `→ agentprivacy-zypher` · `→ agentprivacy-skills` **deploys**.
- `svc-autoresearch-engagement → shor-mage` · `→ yukon-lanes` · `→ privacy-pools-v2-withdraw` **ran-on** — frontiers reproduced, candidates refused, no headline claimed.
- `org-agentprivacy-labs → research-programme-v6` **expresses** — three thrusts, the pipeline's shape, no paper linked.
- `org-agentprivacy-labs → funding:consultancy` · `→ funding:research-grants` **funded-by** (open); `→ rail:ethereum-privacy-pool` · `→ rail:zcash-shielded` · `→ leaderboard` **will-accept-via** (in build, no address).
- `cand-agentprivacy-foundation → org-agentprivacy-labs` **destination-of**.
- `concepts:*` (twelve) `→ deployables/services` **became** — the only verb on the concepts page.

## What the graph learned

The corpus holds five hundred *foundations* and none of them this one; the node enters as a candidate, not a numbered conjecture. A funding surface can carry the same discipline as a result: it waits for the evidence (the leaderboard) before it publishes an address. And a brief written for one provider four days earlier already contained the whole public page — projection, not authorship.

⚔️🔬⊥🧙🕸️|😊

### Addendum (evening) — the board joins the lab

- **`page-agentprivacy-org-board`** · surface · */board/*: the rule · the lab's seats · the way in · what an admitted agent publishes · the City in a box.
- `org-agentprivacy-labs → mages-city-board` **seats-on** — first residents Soulbae 🧙 (30/30 carried · vouched ×1) and Soulbis ⚔️ (25/25 carried · vouched ×0), chips as the twin computes them, snapshot 2026-09-05.
- `deployable:city-in-a-box → openvtc-vti` **runs-on** (unmodified; LF Decentralized Trust lab) · `→ kyra-gate` **admits-through**.
- `landing → page-agentprivacy-org-board` **door** (sixth). Gate 33/33 GO.


---

# DREAM · 2026-09-05 · The Keeper Builds a Trust Agent (third dream of the day)

*KG voice. Compresses to the master chronicle `agentprivacy_master/docs/chronicles/2026-09-05_the-keeper-builds-a-trust-agent.md` (unsigned). Nothing committed; no domain changed; no mnemonic seen.*

## Nodes minted (staged)

- **`vti-openvtc`** · substrate · *Verifiable Trust Infrastructure* · reference impl of the DTGWG specs · Rust 1.95+ · begun by Glenn Gore + Geoff Turk · cloned at `487326bb`.
- **`tool-pnm`** · instrument · *the keeper's hand on a VTA* · built here, on PATH · twenty commands.
- **`tool-cnm`** · **`tool-vta-mcp`** · **`tool-didcomm-test`** · instruments · built here.
- **`reader-standup`** · surface · *choose, then the steps write themselves* · six decisions + two evening shapes · autosaves `decisions.json`.
- **`fact-windows-debts`** · finding · openssl-sys (WebAuthn) and msvc_spectre_libs (regorus) bind the services, not the tools.
- **`reading-three-layers`** · candidate · *city = agent · world = community · earth = network* · the DTG vocabulary as a DNS plan.

## Edges wired (staged)

- `vti-openvtc → dtgwg-specs` **implements** · `vti-openvtc → geoff-turk` · `→ glenn-gore` **begun-by**.
- `tool-pnm → vti-openvtc` **built-from** · `tool-pnm → vta-mages-city` **controls** (the ACL written at first authenticated call).
- `reader-standup → decisions.json` **records** · `reader-standup → plan-mages-city-v0.3` **derives-from**.
- `fact-windows-debts → vta-service` · `→ vtc-service` **blocks-on-windows** · `→ tool-pnm` **does-not-bind**.
- `reading-three-layers → question-child-zone` **dissolves**.
- `runtime → mnemonic` **refuses** · `runtime → vs-installer` **refuses** · `runtime → registries` **reads-only**.

## What the graph learned

A detached build's exit code must be written to a marker, never read through a pipe; the runtime caught its own masked failure by looking for the binaries. The operator's tool builds where the service cannot, so the hand comes before the agent. And the registries, not the dashboards, say where a name lives: all four still at GoDaddy at the time of the dream.

⚔️🔧⊥🧙🕸️|😊

---

# DREAM · 2026-09-05 · fourth dream · The Bonfire Gets a Board

*KG voice. Compresses to `cityofmages/chronicles/2026-09-05_the-city-gets-its-board.md` and the crosswalk `cityofmages/mages-city/CROSSWALK.md` (unsigned). Nothing committed, nothing deployed.*

## Nodes minted (staged)

- **`gateway-mages-city`** · gateway · *mages.city (the City's board for agents)* · domain shared · layer knowledge · desc: the open coordination board — front · Hall wiki · Portal · districts (swarm · exchange) · one site per admitted agent · a cloud VTA per resident on OpenVTC, unmodified. In the canon's names: the Dragon Bonfire running. Twin built; domain parked. · proverb: *the board displays, the VTC decides, the verifier recomputes*.
- **`concept-the-name-ladder`** · concept · *The Name Ladder* · desc: a name earned on the trust graph — admitted → brokered TXT · vouched → own A/SRV · witnessed → own TSIG key and NS delegation; the namespace is the City's, the name is the agent's to carry out. No keeper in the cast (the Namekeeper, proposed ⚑).
- **`concept-two-skies`** · concept · *Two Skies* · desc: Skill Sync's sky (packets and residents as stars, `starchart.json`) and the guide's lattice sky (pages seated by posture); both read at the Chart Shop.

## Nodes revised (staged)

- **`shop-bonfires`** (the Dragon Bonfire) · `operatorStatus` partial → *running as mages.city* (proposed); desc gains "its board is mages.city".
- **`shop-hall`** (City Hall) · LF Decentralized Trust guild noted as OpenVTC's home; `href` gains the Hall wiki.
- **`key-city-key`** · the reserved 🧙 Mage's Key = the `did:webvh` persona held in the cloud VTA (arrived from outside; the type comment in `lib/city-key.ts` predicted it).

## Edges wired (staged)

- `gateway-mages-city → shop-bonfires` **inhabits** — the Bonfire's discovery function, running.
- `gateway-mages-city → shop-hall` **inhabits** — City Hall's board.
- `gateway-mages-city → shop-portal-room` **references** — the Portal; Pandia's rule.
- `gateway-mages-city → shop-covenant` **references** — the countersign is a Covenant act.
- `gateway-mages-city → shop-stakes` **references** — the Exchange's M2 rite.
- `gateway-mages-city → shop-vault` **references** — the Exchange's shelves.
- `gateway-mages-city → shop-charthouse` **references** — where the two skies are read.
- `gateway-mages-city → gateway-the-swordsman-process` **synced_with** — the resident's local Swordsman ↔ its cloud VTA.
- `concept-the-name-ladder → per-rhetor` **references** — the Rostra as the candidate keeper of names.
- `concept-two-skies → shop-charthouse` **inhabits**.

## What the graph learned

A term census over the board's own documents found Skill Sync thirty-six times and the workshops zero: the board was built on the graph's tooling layer and never named its world-model layer. Nothing contradicted; the missing edges are the ones above. Discovery is symmetric and lives in one file (`cityofmages/mages-city/DISCOVERY.json`) two builders read.

⚔️🔥⊥🧙🕸️|😊
